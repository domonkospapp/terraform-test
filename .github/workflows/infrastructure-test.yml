name: learn-github-actions
on: [push]
jobs:
  run-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run test
        working-directory: ./test/bash/
        run: ./test.sh
