{
  "name": "Terraform with AWS",
  "build": {
    "dockerfile": "Dockerfile",
  },
  "postCreateCommand": "aws configure set aws_access_key_id $AWS_ACCESS_KEY && aws configure set aws_secret_access_key $AWS_SECRET && terraform init"
}
